import styles from '../styles/dashboard.module.css';

export default function AddBatch() {
    return (
        <div className={styles.batchContent}>
            <h2>Add Batch</h2>
            {/* Add batch form goes here */}
        </div>
    );
}